{
  "name": "xyz-domain-generator",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^7.2.1",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.15.0"
  }
}